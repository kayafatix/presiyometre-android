{
  "name": "presiyometre-mobil",
  "version": "1.0.0",
  "description": "Presiyometre Deney Raporu - Mobil Uygulama",
  "main": "src/index.html",
  "scripts": {
    "build": "echo 'Static app - no build needed'",
    "cap:init": "npx cap init presiyometre-mobil com.haninsan.presiyometre --web-dir src",
    "cap:android": "npx cap add android",
    "cap:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/filesystem": "^5.2.0",
    "@capacitor/share": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
